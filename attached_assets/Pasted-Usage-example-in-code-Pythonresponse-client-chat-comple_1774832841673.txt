Usage example in code:
Pythonresponse = client.chat.completions.create(
    model="gpt-4o",  # or your preferred model
    messages=[
        {"role": "system", "content": AMERICA_FIRST_SMALL_BIZ_PROMPT},
        {"role": "user", "content": f"Analyze this content from an America First small business perspective. Include relevant FBI crime trends if applicable:\n\n{content_text}\n\nContext data: {context_dict}"}
    ]
)
2. Updated Python Snippet – BLS + Census BFS + FBI Crime Data
This version adds a simple FBI Crime Data pull (using the public USA.gov Crime API wrapper, which is the most accessible endpoint). It returns national-level violent and property crime rates/trends where available.
Pythonimport requests
import json
from datetime import datetime

def get_bls_unemployment(start_year="2023", end_year=None):
    if end_year is None:
        end_year = datetime.now().year
    headers = {'Content-type': 'application/json'}
    data = json.dumps({
        "seriesid": ["LNS14000000"],  # National unemployment rate (seasonally adjusted)
        "startyear": start_year,
        "endyear": str(end_year)
    })
    response = requests.post('https://api.bls.gov/publicAPI/v2/timeseries/data/', data=data, headers=headers)
    if response.status_code == 200:
        json_data = response.json()
        if json_data.get('status') == 'REQUEST_SUCCEEDED':
            series = json_data['Results']['series'][0]
            latest = series['data'][0]
            return {
                "unemployment_rate": float(latest['value']),
                "period": f"{latest['year']}-{latest['period']}"
            }
    return {"error": "Failed to fetch BLS data"}

def get_census_bfs():
    # Placeholder — in production, download latest CSV from census.gov/econ/bfs and parse
    try:
        return {
            "recent_business_applications": "Latest monthly figure from Census BFS (update from official release)",
            "projected_formations": "Latest from Census BFS",
            "note": "Business applications and formations trend"
        }
    except:
        return {"error": "Failed to fetch Census BFS data"}

def get_fbi_crime_data():
    # Public FBI Crime API via usa.gov wrapper (national estimates where available)
    # For more granular data, register for a free API key at api.usa.gov and use /cde endpoints
    try:
        # Example: national violent + property crime trends (adjust endpoint as needed)
        url = "https://api.usa.gov/crime/fbi/sapi/api/summarized/estimates/national"  # National summary example
        response = requests.get(url)  # Add ?API_KEY=your_key for authenticated calls
        if response.status_code == 200:
            data = response.json()
            # Extract key fields (structure varies; adapt to actual response)
            return {
                "violent_crime_rate": data.get("violent_crime_rate_per_100k", "N/A"),
                "property_crime_rate": data.get("property_crime_rate_per_100k", "N/A"),
                "trend": "Latest available trend from FBI Crime Data Explorer (2024-2025 data)",
                "note": "Lower crime supports safer business districts, lower insurance, more customer traffic"
            }
        else:
            return {"error": f"API returned {response.status_code}"}
    except Exception as e:
        return {"error": f"Failed to fetch FBI data: {str(e)}"}

# Combined context for AI
def get_economic_crime_context():
    bls = get_bls_unemployment()
    bfs = get_census_bfs()
    fbi = get_fbi_crime_data()
    
    return {
        "bls": bls,
        "bfs": bfs,
        "fbi_crime": fbi,
        "guidance": "Feed this to the AI with the America First Small Biz prompt. Highlight how lower crime benefits small businesses (safer shopping, lower costs, better hiring)."
    }

# Example usage
context = get_economic_crime_context()
print(json.dumps(context, indent=2))
Notes on FBI integration:

The public endpoints (via usa.gov/crime/fbi) give national/state summaries. For deeper NIBRS data, sign up for a free API key at api.usa.gov and use endpoints like /cde/agencies or /summarized.
In production, cache results (crime data updates monthly/quarterly) and handle the April 2026 pause mentioned in recent FBI notes.
Tie-in example for AI: "This EO strengthens law enforcement → FBI data shows violent crime rate down X% in similar periods, helping small retailers reduce losses and insurance premiums."

How to Use This in Your Flow

When a new article or EO comes in, call get_economic_crime_context().
Pass the raw text + context into the updated AMERICA_FIRST_SMALL_BIZ_PROMPT.
Display the generated summary on the page with a note like: "America First Small Business Analysis — includes BLS, Census BFS, and FBI crime trends."

This makes Emmett Echo a true value-add site: real data + America First small-business framing.